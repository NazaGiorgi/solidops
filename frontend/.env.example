# =============================================================================
# Frontend env. Copy to frontend/.env.local in dev, or use compose env_file.
# NEXT_PUBLIC_* vars are inlined at build time (browser needs them).
# =============================================================================
NEXT_PUBLIC_API_URL=http://localhost:4000
NEXT_PUBLIC_INTERNAL_API_URL=http://backend:4000
