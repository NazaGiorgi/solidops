const {io}=require("socket.io-client");const t=process.argv[2];const s=io("http://backend:4000",{auth:{token:t},transports:["websocket"]});let n=0;const iv=setInterval(()=>{["/api/tickets","/api/notifications/unread-count","/api/dashboard/my-day","/api/customers","/api/technicians"].forEach(p=>fetch("http://backend:4000"+p,{headers:{Authorization:"Bearer "+t}}).then(r=>r.text()).catch(()=>{}));if(++n>300){clearInterval(iv)}},125);setInterval(()=>s.emit("ping"),800);setTimeout(()=>process.exit(0),75000);
