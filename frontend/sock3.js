const { io } = require("socket.io-client"); const token = process.argv[2]; const socket = io("http://backend:4000", { path:"/socket.io", auth:{token}, transports:["websocket"] }); socket.on("connect", ()=>console.log("CONNECTED "+socket.id)); socket.on("connect_error",(e)=>console.log("CONNECT_ERROR "+e.message)); setTimeout(()=>{console.log("still-connected"); process.exit(0)}, 20000);
